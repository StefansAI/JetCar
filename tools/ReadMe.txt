The bin folder contains Windows executables, that can be downloaded directly and tested with example data.
The source folder contains the C# projects and all source codes for Visual Studio 2019.
The Jupyter notebook can be used to train a new model in Colab for instance.